useful commands
.tables
.schema table

CREATE TABLE tracks (
	id integer primary key, 
	path text not null,
	artist text,
	title text,
	album text,
	genre text,
	source text
);
create unique index index_path on tracks(path);
create index index_artist on tracks(artist);
create index index_title on tracks(title);
create index index_album on tracks(album);




DROP TABLE tracks;



